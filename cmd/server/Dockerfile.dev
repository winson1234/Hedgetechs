# Development Dockerfile for Go backend with hot-reloading
FROM golang:1.24-alpine

# Install dependencies: git (for Go packages), ca-certificates (for HTTPS), Air (hot-reload)
# Pin Air to v1.61.1 (compatible with Go 1.24)
RUN apk add --no-cache git ca-certificates && \
    go install github.com/air-verse/air@v1.61.1

# Create non-root user for security
RUN addgroup -g 1001 -S appgroup && \
    adduser -u 1001 -S appuser -G appgroup

# Set working directory
WORKDIR /app

# Copy go mod files and download dependencies
# This layer will be cached unless go.mod or go.sum changes
COPY go.mod go.sum ./
RUN go mod download

# Create tmp directory for Air hot-reload binaries
RUN mkdir -p /app/tmp && chown -R appuser:appgroup /app

# Switch to non-root user
USER appuser

# Source code will be mounted as a volume for hot-reloading
# No need to COPY source code here

# Expose port
EXPOSE 8080

# Run Air for hot-reloading (Air will use .air.toml from mounted volume)
CMD ["air"]
