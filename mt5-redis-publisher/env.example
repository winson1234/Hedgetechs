# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Redis Pub/Sub channel name (must match Go backend configuration)
REDIS_CHANNEL=fx_price_updates

# Redis Hash key for storing latest prices (must match Go backend configuration)
REDIS_HASH_KEY=fx_latest_prices
